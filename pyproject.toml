[project]
name = "qfl-mini"
version = "0.1.0"
description = "A minimal execution sandbox for federated quantum-classical workloads."
requires-python = ">=3.10"
dependencies = ["pennylane"]

[tool.setuptools.packages.find]
include = ["qflmini*"]
